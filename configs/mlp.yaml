model: mlp

model_params:
  hidden_dims: [512, 256]
  dropout: 0.20

training:
  epochs: 30
  batch_size: 128
  learning_rate: 0.001
  weight_decay: 0.0001
  gradient_clip: 1.0
  class_weight: true
  num_workers: 0
